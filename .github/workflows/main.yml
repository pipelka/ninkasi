name: CI

on: 
  push:
    branches: 
      - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: installing PlatformIO
      run: |
        sudo pip install platformio
        platformio update
    - name: Build firmware
      run: |
        platformio run
